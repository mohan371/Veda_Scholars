# ============================================
# Veda Scholars Backend Environment Variables
# ============================================
# INSTRUCTIONS:
# 1. Copy this file to .env: cp env.template .env
# 2. Uncomment the section you need (DEVELOPMENT or PRODUCTION)
# 3. Comment out the section you don't need
# 4. Update the values with your actual configuration
# 5. .env is gitignored and will not be committed
# ============================================

# ============================================
# DEVELOPMENT ENVIRONMENT (Local Machine)
# ============================================
# Uncomment this section for local development

# Server Configuration
NODE_ENV=development
PORT=8483

# MongoDB Connection (Local MongoDB)
MONGODB_URI=mongodb://localhost:27017/veda-scholars
# OR if using MongoDB Atlas (Cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster-name.mongodb.net/veda-scholars?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-development-secret-key-change-this-min-32-characters
JWT_EXPIRES_IN=7d

# Email Configuration (SMTP)
# For development, you can use Gmail SMTP or a service like Mailtrap
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="Veda Scholars" <noreply@vedascholars.com>

# Email Recipients
CONTACT_EMAIL=enquiries@vedascholars.com
PARTNERS_EMAIL=partners@vedascholars.com

# Google OAuth (Development)
GOOGLE_CLIENT_ID=your-google-client-id-for-development.apps.googleusercontent.com

# ============================================
# PRODUCTION ENVIRONMENT (Server)
# ============================================
# Comment out DEVELOPMENT section above and uncomment this section for production

# Server Configuration
# NODE_ENV=production
# PORT=8483

# MongoDB Connection (Production - Use MongoDB Atlas)
# MONGODB_URI=mongodb+srv://username:password@cluster-name.mongodb.net/veda-scholars?retryWrites=true&w=majority

# JWT Configuration
# Generate a secure random secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# JWT_SECRET=your-super-secure-production-secret-key-min-64-characters
# JWT_EXPIRES_IN=7d

# Email Configuration (SMTP)
# Use your production email service (Namecheap Private Email, Google Workspace, etc.)
# SMTP_HOST=mail.privateemail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=info@vedascholars.com
# SMTP_PASS=your-production-email-password
# SMTP_FROM="Veda Scholars" <info@vedascholars.com>

# Email Recipients
# CONTACT_EMAIL=enquiries@vedascholars.com
# PARTNERS_EMAIL=partners@vedascholars.com

# Google OAuth (Production)
# GOOGLE_CLIENT_ID=your-google-client-id-for-production.apps.googleusercontent.com

# ============================================
# SMTP Configuration Examples
# ============================================
# 
# Namecheap Private Email:
# SMTP_HOST=mail.privateemail.com
# SMTP_PORT=587
# SMTP_SECURE=false
#
# Google Workspace:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_PASS=your-app-password (generate in Google Admin)
#
# Microsoft 365:
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_SECURE=false
#
# Zoho Mail:
# SMTP_HOST=smtp.zoho.com
# SMTP_PORT=587
# SMTP_SECURE=false

